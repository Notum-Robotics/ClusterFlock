# llama.cpp source tree (cloned for building)
llama_cpp/

# Downloaded models (large files)
models/**/*.gguf
models/**/*.gguf.part

# Prebuilt binaries — users build locally via build.sh
build/

# Python caches
__pycache__/
*.pyc

# Runtime config (contains tokens)
cluster.json

# Benchmark cache
benchmarks.json
