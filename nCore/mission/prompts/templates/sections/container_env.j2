{% if container_env %}
=== CONTAINER ENVIRONMENT ===
Ubuntu 24.04 • Python 3.12 • Node.js 18+ • git • npm • curl • wget • jq • ctags
Pre-installed Python packages: fastapi, uvicorn, pytest, httpx, requests, flask,
  aiohttp, beautifulsoup4, lxml, pyyaml, toml, markdown, jinja2
Install more: python3 -m pip install --break-system-packages <pkg>
Pre-installed tools: outline, lint, test, search_def, verify, diff_since
⚠ Do NOT waste iterations installing packages that are already available.
{% endif %}
